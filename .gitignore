# 個人設定（含真實信箱/路徑），只 commit config.env.example
config.env

# 執行紀錄
*.log
run.log

# Python 快取
__pycache__/
*.pyc

# 每週期「已寄」記號（執行期狀態，不進版控）
state/

# 任何可能誤放的機密（密碼一律存 macOS Keychain，不進 repo）
*.env
!config.env.example
secrets*
*app-password*
